# PROOF and TBTX Scrollcraft review

I use the office-handoff PROOF film as the opening and six work-task films as examples. The two longer PROOF concepts remain available on request. The selected cuts keep their accepted wording for this stage; surrounding copy uses Finder.

The review is served at `/review/proof-momentum/index.html`. The eleven newly selected films are hosted in the existing TBTX Blob store under a review path. Their returned URLs and original-file hashes are recorded beside the review. MP4 files are not committed.

`defog-daily-hero.mp4` belongs to B2B / Map. `ddd-r5-picture-sfx.mp4` belongs to B2C / Scan. The rejected 69-second narration, generated refinery hero and It Only Looks Automated design direction are excluded.

The page uses the repository’s actual Scrollcraft runtime. Stand up opens the two entry paths. The personal Scan has 8 questions; the business Map has 15. The result keeps Show the math nested and exposes every answer, point value, total, normalization and band. The selected scene does not affect scoring.

The DDD offer records Erik-funded first-month access and one additional month per optional $7.77 contribution, with Erik matching each contribution. BBM is depicted after recorded BBAI completion.

This is a design review. Accounts, persistence, payments, match allocation, observed Trace evidence, the PROOF pressure engine and server-enforced BBM access are not implemented.

## Validation

The earlier browser pass covered Scrollcraft scrubbing, the gate, both answer totals, answer retention, reduced motion and responsive widths. It preceded the latest approved-film additions. The browser rerun was declined, so playback and layout after those additions remain unverified.

Source checks cover all 23 question IDs, all 92 option texts and values, all 48 attainable totals, band boundaries and invalid-answer handling. No Next.js build, production merge or deployment is claimed.

The full messaging and UX document remains in the separate private PROOF repository.

All eleven newly hosted film URLs returned HTTP 200 with byte sizes matching their source files. This confirms file availability, not browser playback.
